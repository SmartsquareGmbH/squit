SET
@TEST_SETTING = 'ENGLISH';

CREATE TABLE DIALECT_ANIMALS
(
    id   INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(255) NOT NULL
);

INSERT INTO DIALECT_ANIMALS (name)
VALUES ('dog');
INSERT INTO DIALECT_ANIMALS (name)
VALUES ('cat');
