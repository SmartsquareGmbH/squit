endpoint = ${endpointPlaceholder}
mediaType = "application/xml"
preProcessorScripts = [${rootDir}"/src/test/project/pre_process.groovy"]
postProcessorScripts = [${rootDir}"/src/test/project/post_process.groovy"]

databaseConfigurations = [
  {name = "test", jdbc = "jdbc:h2:"${rootDir}"/testDb", username = "test", password = "test"}
]

headers = {
  "some": "header"
}
